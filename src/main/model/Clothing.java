package model;

// A Clothing item that keeps track of its own properties:
// type, colour, times worn
public class Clothing {
    enum Type {
        TOP,
        BOTTOM,
        SHOES,
        HEADWEAR,
        ACCESSORY
    }

}
